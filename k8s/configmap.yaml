apiVersion: v1
kind: ConfigMap
metadata:
  name: api-config
  namespace: default
  labels:
    app.kubernetes.io/name: aws-cloud-platform
    app.kubernetes.io/component: api
data:
  PORT: "8080"
  LOG_LEVEL: "info"
