apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: default
  labels:
    app.kubernetes.io/name: aws-cloud-platform
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
    - name: http
      port: 80
      targetPort: 8080
